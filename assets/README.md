Place your transparent overlay image here as:

assets/overlay.png

The app expects this exact filename/path.

If you replace the image and mobile still shows old one, keep `?v=<new-number>` in `index.html` image src to bust cache.

Place the reunion logo image here as:

assets/logo.png

The captured/downloaded image draws this logo on the left side of the frame footer. If you replace it and mobile still shows the old logo, update the `?v=<new-number>` query in `index.html`.
